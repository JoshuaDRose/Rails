class Version:
    """ Description: Manages rules of a given version.

    Each version has a syntax which is applied as follows: x.y.z where:
        - x is major release
        - y is a feature
        - z is a patch

    Functionality: The version class is responsible for regulating these
    changes and incrementing individual versions as set by the rules explained above.
    """

    def __init__(self):
        ...
